@page "/"
@inject ResidualKnowledgeApp.Client.Services.IProjectsService ProjectsService

@if (!ProjectsService.IsAuthorized)
{
	
}
else
{
	<MudText Typo="Typo.h4">Программа для проверки остаточных знаний студентов</MudText>
	<br />
	<!--  Если не автормзован - перенаправляет на логин -->
	<MudText Typo="Typo.body1">
		Это приложение предназначено для проверки остаточных знаний студентов.
	</MudText>
	<br />
}